## 1. Day2

[투포인터/슬라이딩 윈도우]
- 투포인터는 슬라이딩 윈도우를 포함하는 넓은 개념이다. 
  - "부분합 문제" = 슬라이딩 윈도우
    - 연속 부분배열 / 부분배열의 길이? = 슬라이딩 윈도우
    - 이분탐색으로 가능하긴 하지만, "부분배열의 길이가 달라지고, 내부 요소도 달라진다."
      - 작으면 right ++ 크면 left--
      - 정렬하였다면 마찬가지로 기준값보다 크면 작은값을 빼고 기준값보다 작으면 큰값을 더하는 식.
  - 부분합의 조건이 equals라면?
    - while 조건 내부에 equal 분기처리를 추가한다.

[이분탐색  / 슬라이딩 윈도우]
- "기준 길이", 길이 그 자체 -> 이분탐색, 그 길이만 조정해나가면 됨
- "슬라이딩 윈도우" -> "부분 배열".."배열의 부분합", 기준 그 자체가 될 수 없고, 부분집합/요소가 항상 달라진다.
  - //최소합 -> MAX, 최대합 -> MIN  int answer = Integer.MAX_VALUE;