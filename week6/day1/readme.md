## 1. Day1

[이분탐색]
- Implements : 배운점(문제)
  - 이분탐색 > 상황비교 or 이분탐색1 > 이분탐색2 > 상황비교 or target > 이분 > 이분 > 상황비교
    - 길이가 길면 랜선의 개수는 작아지고, 길이가 작아지면 랜선의 개수는 커진다.
      - 전형적인 이분탐색을 통해 적절한 랜선의 길이 및 전체 길이를 추출가능한 방안 적용.
    - 바위의 개수의 경우 제거할 돌의 개수도 커서, 돌 개수에 대한 모든 경우의 수를 구하기는 불가
      - 브루트포스(완전탐색) 불가능한 상태
      - 애초부터 최소 거리를 정하고, 그 최소거리가 가능한 것인지 역으로 접근하는 이분탐색
        - 다만 distance와 마지막 돌 사이의 거리도 탐색 경우에 포함되어야 한다.
- Improvements : 작동원리/개선포인트(본질)
  - 경계조건 유의 low = high일 경우에도 계속 진행, low > high일 경우에만 반복을 중단한다.
  - target값을 조절해나가는 과정에 유의
    - 조건 만족 ? low = mid + 1, 조건 불만족 ? high = mid - 1
  - 구하고자 하는 target를 조절해간다는 접근으로 해결방안 모색
- Additional : 추가적인 유의점들

int[] 형태의 배열을 복사하는 방법? -> Arrays.copyOf(arrays, N + 1)
- int N = rocks.length;
-  rocks = Arrays.copyOf(rocks, N + 1);
-  rocks[N] = distance;